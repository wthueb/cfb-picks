{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "next-env.d.ts"],
      "env": ["NODE_ENV", "PORT", "SKIP_ENV_VALIDATION"]
    },
    "dev": {
      "persistent": true
    }
  }
}
